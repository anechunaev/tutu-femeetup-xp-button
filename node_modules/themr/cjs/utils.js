/**
 * @license
 * Copyright (C) 2016 Chi Vinh Le and contributors.
 *
 * This software may be modified and distributed under the terms
 * of the MIT license. See the LICENSE file for details.
 */
"use strict";
function resolveCallbackDeep(subject, props, vars) {
    for (var key in subject) {
        var kind = typeof subject[key];
        if (kind === "function") {
            subject[key] = subject[key](props, vars);
        }
        else if (kind === "object") {
            resolveCallbackDeep(subject[key], props, vars);
        }
    }
}
function resolveCallback(subject, props, vars, skip) {
    if (skip === void 0) { skip = []; }
    for (var key in subject) {
        if (skip.indexOf(key) > -1) {
            continue;
        }
        var kind = typeof subject[key];
        if (kind === "function") {
            subject[key] = subject[key](props, vars);
        }
        else if (kind === "object") {
            resolveCallbackDeep(subject[key], props, vars);
        }
    }
}
exports.resolveCallback = resolveCallback;

//# sourceMappingURL=utils.js.map
